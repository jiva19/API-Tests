# API-Tests
C# API suite using RESTSharp and Testcontainers to validate an Order Controller. Features a custom test server with WebApplicationFactory to test RBAC, JWT auth, and middleware. Uses SQL containers for total isolation. Covers CRUD, concurrency, and error handling (400/401/403) to ensure a robust, high-speed, and data-clean testing lifecycle.
